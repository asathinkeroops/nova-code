// The working-spinner verb list moved into the i18n catalog (`t.spinner.workingWords`)
// so it localizes; read it at call time, not here (see the i18n invariant).

export const TOOL_SPINNER_DELAY_MS = 300;
